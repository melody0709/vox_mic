# DPDFNet 后端稳定性修复方案

状态：实施中
范围：DPDFNet worker、epoch 切换、失败状态、输出校验、状态显示和回归测试
当前版本：0.6.4

## 结论与修订

本方案采纳本轮审查结论，但不采用“join 超时后 detach / std::terminate”作为短期修复。`DpdfnetProcessor` 的 worker 仍可能持有 denoiser 和 DLL 中的对象，超时 detach 后释放这些资源会产生 use-after-free；`std::terminate` 也不能改善正常退出体验。

本次优先修复可在当前线程模型内安全处理的问题，并把原生 `Run()` 永久阻塞记录为已知限制。真正的硬隔离方案应是把 DPDFNet worker 放入独立子进程，作为后续架构任务单独评估。

## 优先级

### P0：发布前必须完成

1. 失败状态和 worker 空转
2. epoch 切换首块丢弃竞态
3. failed 状态下的 `setEpoch()` 短路

### P1：发布前建议完成

4. hard-fail 回归测试
5. DPDFNet 输出 NaN/Inf 和异常长度防护
6. 原生 `Run()` 阻塞限制的文档与退出测试

### P2：体验与维护性

7. NR 关闭时 effective backend 状态准确性
8. 状态文本单次重绘
9. `expectedFrameShift` 降为 `prepare()` 局部变量
10. 非 480 samples 路径警告和状态同步

## 1. Worker failure 状态收敛

涉及：`src/dsp/dpdfnet_processor.cpp`

当前 `failWorker()` 只设置 `failed=true` 并唤醒 worker；worker 之后仍会每 20ms 进入一次循环。修复要求：

- `failWorker()` 设置 `failed=true`，同时设置 `ready=false`；
- worker loop 在处理 epoch、pop 输入前检查 `failed`；
- failed 状态下使用当前 auto-reset `wakeEvent` 无限等待；
- `stopWorker()` 设置 `stop=true` 并唤醒 event，worker 被唤醒后先检查 `stop` 并退出；
- join 完成后统一清理 event、denoiser、DLL，并保持 `ready=false`；
- `isReady()`、`hasFailed()` 和 `setEpoch()` 的状态语义保持一致：failed worker 不再被视为 ready，也不再接受 epoch 操作。

注意：不能把 event 改成 manual-reset 后直接无限等待而不 `ResetEvent()`，否则会形成立即返回的忙循环。

## 2. 修复 epoch 首块丢弃竞态

涉及：`src/dsp/dpdfnet_processor.cpp`

当前 worker 先读取 `requestedEpoch`，再 pop 输入；render 线程可能在两步之间提交新 epoch 首块，导致该 block 被旧 `workerEpoch` 判断为 stale 并丢弃。

处理规则：

- 输入 block 的 `epoch` 作为该 block 的权威标签；
- pop 后发现 `input.epoch < workerEpoch`：丢弃，属于旧 epoch；
- pop 后发现 `input.epoch > workerEpoch`：先对 `input.epoch` 执行 `resetWorkerState()`，然后继续处理当前 block；
- 如果 `requestedEpoch` 已经更新到更晚 epoch，当前 block 属于已被 superseded 的 epoch，可以丢弃；实现必须在处理前再次比较，避免处理已经被后续 reset 覆盖的 block；
- 推理返回后继续保留现有的 requested epoch 二次检查，旧结果不得进入 output FIFO；
- 不恢复 reset 时清空 input queue 的做法，继续依靠 epoch 标签过滤旧输入。

实现时应明确 SPSC 队列的 producer/consumer 约束，不增加 render 与 worker 之间的锁。

## 3. Hard-fail 输出验证

涉及：`src/dsp/dpdfnet_processor.cpp`

在 worker 线程、调用 `appendOutput()` 之前验证：

- `audio->sample_rate == expectedSampleRate`；
- `audio->n >= 0`；
- `audio->n == 0` 时允许空输出；
- `audio->n > 0` 时 `audio->samples != nullptr`；
- 输出长度不超过经过 smoke/运行时观测确认的安全上限；第一版可用 `DPDFNET_BLOCK_SAMPLES * 4`（1920 samples）作为暂定阈值，若实测模型存在合法更大输出再调整；
- 所有输出 sample 都是 finite，拒绝 NaN/Inf。

异常响应统一销毁 audio 对象后调用 `failWorker()`，不得把坏数据送入 output FIFO。

## 4. Hard-fail 和生命周期测试

涉及：`tests/`、`CMakeLists.txt`、`build.bat`

在 `VOXMIC_DPDFNET_TEST_HOOKS` 下增加可控 failure hook，至少覆盖：

- worker 返回异常 sample rate；
- worker 返回非法长度或空 sample 指针；
- worker 返回 NaN/Inf；
- `failWorker()` 后 `isReady()==false`、`hasFailed()==true`；
- fail 后调用 `setEpoch()` 不再更新/唤醒失败 worker；
- fail 后析构在正常可返回的 worker 场景下应小于 100ms，不发生 20ms 空转；
- reset 与新 epoch 首块高频交错时，首块不会因旧 `workerEpoch` 被误丢弃。

保留并继续运行现有测试：

- `dpdfnet_smoke`；
- `dpdfnet_fallback_smoke`；
- `dpdfnet_pipeline_switch_smoke`；
- RNNoise-only 构建、runtime layout、`git diff --check`。

测试中不以 Sleep 作为唯一竞态证明，增加紧密 reset/push 循环和必要的 test hook/计数器。

## 5. 原生 Run() 永久阻塞风险

涉及：`src/dsp/dpdfnet_processor.cpp`、README、ARCHITECTURE、CHANGELOG、AGENTS

当前 watchdog 能处理“Run 返回但没有输出”，不能处理中间 DLL 调用永久不返回的情况；此时 render 可降级，但析构 join 仍可能等待不返回。

本次短期处理：

- 不使用 detach、强制终止线程或超时后释放 denoiser/DLL；
- 在开发文档中明确记录：原生 API 卡死时，音频路径可降级，但进程退出可能受 worker join 影响；
- 自动化测试只覆盖 Run 返回异常值（sample rate、长度、NaN/Inf）；Run 永久不返回不能放入普通自动化测试，改为文档化已知限制并通过人工/隔离实验验证；
- 增加正常退出、高负载退出、模型加载异常和 DLL 异常场景测试；
- 记录后续方案：DPDFNet adapter 放入独立子进程，通过 IPC/共享内存传输 480-sample block，子进程可被安全回收。

如果后续必须实现退出超时，应先完成资源所有权和进程隔离设计，不能直接 detach 当前 worker。

## 6. NR 关闭时的 effective backend 状态

涉及：`src/dsp/pipeline.h`、`src/main.cpp`、`src/settings_dialog.cpp`

当前 NR 关闭时仍可能把 `g_denoiseEffectiveBackend` 设置成 DPDFNet，但实际 pipeline 在 `!nrOn` 分支直接跳过降噪。

建议增加 `DenoiseBackendKind::Off = 2`，并满足：

- NR 关闭时 effective backend 为 Off；
- Stats 显示 `denoise=off`；
- Settings 显示“Noise reduction is disabled”，不误报 DPDFNet 正在运行；
- 同步修改 `main.cpp:116-120` 的 Stats 映射、`settings_dialog.cpp:157-160` 的状态分支，以及所有测试中的 effective backend 判断；不得继续使用“不是 DPDFNet 就当 RNNoise”的隐式映射；
- 重新打开 NR 后，按请求 backend 和可用性重新选择 RNNoise/DPDFNet；
- 不改变 `DenoiseBackend` 配置持久化格式，配置仍只保存 `rnnoise|dpdfnet`。

## 7. UI 状态重绘优化

涉及：`src/settings_dialog.cpp`

保留当前 owner-draw 背景填充机制，避免文字残留；将清空和设置文本的两次立即重绘合并为一次受控更新，优先采用 `WM_SETREDRAW` 或单次 invalidate/update。验收标准是：短文本替换长文本、长文本替换短文本，以及 DPDFNet/RNNoise 快速切换均无残影、无闪烁、无重叠。

## 8. 清理与非 480 路径

涉及：`src/dsp/dpdfnet_processor.cpp`、`src/dsp/pipeline.h`

- 将只在 `prepare()` 契约检查中使用的 `expectedFrameShift` 从 Impl 成员降为局部变量；
- 对 `numSamples != 480` 的 fallback 路径增加一次性诊断日志或 debug 断言；
- 明确该路径当前不会运行 RNNoise；如果未来允许变长 block，应引入 accumulator，而不是静默跳过去噪；
- 如保留 fallback，确保 settings/epoch 状态不会因提前 return 而长期滞后。

## 9. 版本与实施顺序

正式修改代码前先升级版本，避免污染当前 0.6.4 产物：

- `src/version.h`：升级到下一个补丁版本；
- `android_app/app/build.gradle`：同步 `versionName`；
- `versionCode` 加 1；
- 不手工修改 CMake/MSI 派生版本。

实施顺序：

1. 版本升级；
2. worker failure 状态和 `setEpoch()`；
3. epoch 首块竞态；
4. 输出 finite/长度验证；
5. failure/lifecycle/epoch stress 测试；
6. NR Off 状态；
7. UI 重绘和字段清理；
8. 文档更新；
9. RNNoise-only 与 DPDFNet 构建、全部 smoke、layout、退出和打包验证。

## 验收标准

- failed worker 不再 20ms 空转；
- failed worker 的 `isReady()` 为 false，`setEpoch()` 无效且不会触碰失败 session；
- 高频 epoch reset 下新 epoch 首块不被误丢弃；
- 旧 epoch 输出不会进入新 epoch FIFO；
- NaN/Inf/非法音频响应触发 hard-fail 并安全回退 RNNoise；
- render 线程仍不执行阻塞 DLL/模型 I/O；
- NR 关闭时 Stats/Settings 显示 Off 而不是 DPDFNet；
- `DenoiseBackendKind::Off` 的所有生产和测试消费者均能正确显示/断言 Off；
- UI 状态文本切换无重叠和明显闪烁；
- RNNoise-only 构建仍只有 exe/manifest；
- DPDFNet 构建、fallback、切换、failure、退出测试全部通过；
- `build/` 清理后仍可从 `third_party/dpdfnet` 重新生成依赖；
- 版本和打包输入摘要一致，未覆盖 0.6.4 产物。

## 实施约束补充

- `failWorker()` 的原子状态更新顺序固定为 `failed=true` → `ready=false` → 唤醒 event；
- 不使用 `detach`、强制终止线程或 `std::terminate` 解决 `Run()` 阻塞；
- epoch 修复必须保留“输入 block 标签判断”和“推理返回后的二次 requested epoch 判断”两道保护；
- hard-fail 的输出上限先采用 1920 samples，所有阈值变更必须同步 smoke 测试和文档。

## 实施状态

已完成（2026-08-10）：

- 版本已升级到 `0.6.5`，Android `versionCode=11`；
- worker failure 收敛、epoch 首块处理和输出合法性校验已实现；
- 增加 failure smoke，并通过 DPDFNet、fallback、pipeline switch、failure 全部测试；
- NR Off 状态、Settings 文本重绘和非 480 block 警告已同步；
- README、架构、变更记录、构建/开发文档已更新；
- RNNoise-only 构建、DPDFNet 构建、runtime layout 和 0.6.5 Portable/MSI 打包均已验证；
- `Run()` 永久阻塞仍作为已知限制记录，未采用 detach、强杀线程或 `std::terminate`。

验收结果：通过。

### 收尾轻量优化

- 在测试构建中增加 `setEpoch()` 短路计数器，failure smoke 直接断言失败后 10 次调用全部被拦截；
- 为 worker 自身调用 `failWorker()` 时的 auto-reset event 信号行为补充注释；
- 未改变生产运行逻辑，DPDFNet 全量 smoke 测试重新通过。
